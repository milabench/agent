# Localhost inventory for local development deployment
[local]
localhost ansible_connection=local ansible_become=true ansible_become_method=sudo

[local:vars]
# ansible_become_password_file=~/.ansible_become_password
# Uncomment and set if you need to customize
agent_deployment_mode=dev
milabench_base=/opt/milabench
agent_user=ciara
